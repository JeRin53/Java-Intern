class User{
         hashMap<Integer,String> users = new HashMap<>();
         users.put(12,"Sam")
         users.put(13,"Roshan")
         users.put(12,"Sam") //duplicate remove
         Set<String> uniqueKeys = new HashSet<users.uniqueKeys>();
         if(users.uniqueKeys % 2 == 0 )
         {
               System.out.println("Users having even Id no. : "+ users.uniqueKeys()); 
         }
         else{
                System.out.println("Users having odd Id no. : "+ users.uniqueKeys());
         }
        }
